add_executable(test_protocol test_protocol.cpp)
target_link_libraries(test_protocol PRIVATE goat_vesc)
goat_vesc_enable_warnings(test_protocol)
goat_vesc_enable_sanitizers(test_protocol)
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
  target_compile_options(test_protocol PRIVATE -UNDEBUG)
endif()
add_test(NAME test_protocol COMMAND test_protocol)
goat_vesc_configure_sanitizer_test(test_protocol)

add_executable(test_client test_client.cpp)
target_link_libraries(test_client PRIVATE goat_vesc)
goat_vesc_enable_warnings(test_client)
goat_vesc_enable_sanitizers(test_client)
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
  target_compile_options(test_client PRIVATE -UNDEBUG)
endif()
add_test(NAME test_client COMMAND test_client)
set_tests_properties(test_client PROPERTIES TIMEOUT 10)
goat_vesc_configure_sanitizer_test(test_client)
