/**
 * @file errors.hpp
 * @brief Placeholder for future library-specific error types.
 *
 * The current library surface reports operational failure through boolean
 * return values and `std::optional` results rather than dedicated error
 * objects. This header is installed so downstream code has a stable include
 * location if richer error reporting is introduced later.
 */

#pragma once
